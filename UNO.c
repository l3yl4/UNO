#include <stdio.h>

#include "partida.h"

int main()
{
    tUNO p;
    
    //inicializar_azar();
    inicializar_partida(&p);
    /*mostrar_estado_partida(partida);
    while(!fin_partida(partida)) 
    {
        realizar_jugada(&p);
        esperar();
        mostrar_estado_partida(partida);
        if (!fin_partida(partida))
            pasar_turno(&partida);
    }
    mostrar_ganador(partida);
    esperar(); */
}
   



